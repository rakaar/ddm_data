# VBMC Fitting aborts after realizing right LED time
- `LED_off_on_data_fit_aborts_with_trunc_and_censor.ipynb` - LED off, data, Left trunc, right censor
- `vbmc_V_A_step_jump_fit_LED_ON_data.ipynb` - LED on, V_A step jump, data, Left trunc, right censor
- `compare_V_A_step_LED_on_vanila_LED_off_with_data.ipynb` - compare LED off and LED on aborts fit with Left trunc and right censor
- `vbmc_ONLY_step_jump_fit_LED_ON_data.ipynb` - fit only post LED drift, other pararms are same as that of LED off fit
- `compare_ONLY_V_A_step_LED_on_vanila_LED_off_with_data.ipynb` - Compared LED off and LED on post-LED drift fit
- `V_A_step_jump_fit_censor_post_LED_sim_data.ipynb` - Post LED censoring on simulated data
- `V_A_step_jump_fit_censor_post_LED_real_data.ipynb` - Post LED censoring on real data, Able to fit Post LED aborts with step jump change in drift
- `V_A_step_jump_fit_censor_post_LED_real_data.py` - python file, same as above
- `post_LED_drift_and_motor_fit_VBMC.ipynb` - LED on data, fits post-LED drift and motor delay (2 params), V_A and theta_A fixed
- `aborts_animal_wise_explore.py` - plots abort rate vs LED onset time per animal for LED ON/OFF, with peak time and area calculations. **Includes permutation test for abort-fraction differences across LED ON conditions (left/right/bilateral inhibition)**
- `less_than_300_LED7.py` - analyzes aborts with timed_fix < 300ms for LED7 session across animals
- `explore_animal_variability_LED_ON.py` - **NEW: explores animal-by-animal variability in LED ON abort patterns and statistics**
- `plot_vbmc_param_summary_per_animal.py` - **NEW: generates summary plots of VBMC parameter estimates per animal for LED ON fits**
- `simulate_and_fit_proactive_all_at_once.py` - Simulates proactive process with single bound accumulator, drift change after LED onset, and compares theoretical vs simulated RT distributions for LED ON/OFF trials
- `testing_v1_proactive_LED_theory.py` - Same as above but with left truncation (T_trunc=0.6s) and censoring verification. Includes theoretical PDF calculations with truncation, Monte Carlo averaging over (t_stim, t_LED) pairs, and survival probability calculations for censoring validation
- `test_vbmc_proactive_led_likelihood.py` - Simulates proactive LED ON/OFF data, defines truncation/censoring log-likelihood, and validates it via histogram vs theory plus censoring checks
- `vbmc_simulated_data_proactive_LED_fit.py` - VBMC parameter recovery on simulated proactive LED data. Simulates 3000 trials (V_A_base=1.8, V_A_post_LED=2.4, theta_A=1.5, t_aff=40ms, t_effect=35ms, motor_delay=50ms, T_trunc=0.3s), fits all 6 params with trapezoidal priors, outputs corner plot, RTD comparison, and RT-wrt-LED histograms
- `vbmc_real_data_proactive_LED_fit.py` - VBMC fitting of real animal data with proactive LED model. Set ANIMAL_IDX at top to select animal. Fits all 6 params, outputs corner plot, RTD comparison (data vs theory vs sim), and RT-wrt-LED histograms (data vs sim). Uses trial index sampling to preserve (t_LED, t_stim) correlation
- `vbmc_real_data_proactive_LED_fit_all_animals_agg.py` - Same as above but aggregates data from ALL animals instead of fitting a single animal. Outputs vbmc_real_all_animals_*.pdf/pkl files
- `vbmc_real_data_proactive_LED_fit_all_animals_agg_CORR_ID_no_trunc_exp_lapse.py` - Aggregates data from ALL animals, uses CORR_ID logic, no truncation, exponential lapse.
- `vbmc_real_data_proactive_LED_fit_all_animals_agg_CORR_ID_no_trunc_exp_lapse_PYDDM.py` - **NEW: PyDDM-based implementation of proactive model. Uses PyDDM FP solver for likelihood computation with linear drift increase and linear bound decrease after LED effect time. Includes exponential lapse process mixed with proactive process, right-censored trials at t_stim, and supports both individual animal and aggregated fitting via ANIMAL_ID flag.**
- `vbmc_real_data_proactive_LED_fit_all_animals_agg_CORR_ID_no_trunc_exp_lapse_PYDDM_FASTER.py` - **NEW: Optimized version of PYDDM implementation. Uses direct analytical formulas for LED OFF trials (no PyDDM solving needed) while keeping PyDDM for LED ON trials only. Significant speedup by avoiding unnecessary drift/bound slope computations for OFF trials.**
- `vbmc_real_data_proactive_LED_fit_all_animals_agg_CORR_ID_no_trunc_exp_lapse_PYDDM_FASTER_only_bound_drop.py` - **NEW: Variant of PYDDM_FASTER with bound drop that saturates. Uses PyDDM for LED ON trials with linear bound decrease after LED effect time until saturation point (theta_A_saturate). Includes 8 parameters: V_A_base, theta_A, bound_slope, theta_A_saturate, del_a_minus_del_LED, del_m_plus_del_LED, lapse_prob, beta_lapse.**
- `vbmc_real_data_proactive_LED_fit_all_animals_agg_CORR_ID_no_trunc_exp_lapse_PYDDM_FASTER_only_bound_drop_no_saturate.py` - **NEW: Variant of PYDDM_FASTER with bound drop without saturation. Uses PyDDM for LED ON trials with linear bound decrease after LED effect time without saturation. Similar parameter structure but without theta_A_saturate constraint, allowing unlimited bound decrease.**
- `plot_data_rt_wrt_led_all_animals.py` - generates data-only RT wrt LED plots for all animals in one row, uses same filtering and abort/truncation logic as vbmc_real_data_proactive_LED_fit_CORR_ID.py, plots LED ON/OFF area-weighted histograms (density * abort fraction), x-range -0.2 to 0.2, bin width 0.05, saves PNG data_rt_wrt_led_all_animals_1x6.png
- `plot_data_rt_wrt_led_trunc_vs_no_trunc_per_animal.py` - **NEW: compares RT wrt LED distributions with vs. without truncation (removing aborts < T_trunc) for each animal in a grid layout.**
- `plot_valid_rtd_cdf_by_abl_abs_ild_stim_segments_multi_animal.py` - **NEW: builds pooled multi-animal valid-trial RTD/CDF diagnostics from `fit_animal_by_animal/batch_csvs`, segments trials into equal-width `intended_fix` bins after an optional upper cap, and saves four figures: RTD/CDF by `ABL x |ILD|` plus RTD/CDF collapsed across ILD with ABL overlays.**
- `plot_valid_rtd_cdf_by_abl_abs_ild_stim_quantiles_multi_animal.py` - UPDATED: Builds pooled multi-animal RTD/CDF diagnostics after splitting trials into configurable `intended_fix` quantile bins. Supports `TRIAL_POOL_MODE = "valid"`, `"valid_plus_abort3"`, or `"valid_plus_abort3_and_4"`, `DATA_DENSITY_MODE = "raw"` or `"valid_conditioned"`, and batch-specific proactive left-truncation that removes early `abort_event == 3` trials before the RTD/CDF/tachometric plots. The early/late-by-ABL RTD overlay now also saves a stable PKL payload with the plotted binned densities for downstream comparisons.
- `plot_valid_mean_rt_vs_stim_quantile_bins_multi_animal.py` - **NEW: pooled `intended_fix` quantile-bin summary for mean `RTwrtStim`. Supports configurable `NUM_STIM_BINS`, includes valid trials plus optional `abort_event == 4`, uses SEM error bars, and saves a combined `1 x 2` figure with `ABL = 20/40/60` overlays on the left and an aggregate curve on the right. Text under each panel reports per-bin stimulus-composition fractions.**
- `plot_valid_accuracy_vs_stim_quantile_bins_multi_animal.py` - **NEW: pooled valid-trial accuracy summary vs `intended_fix` quantile-bin midpoint. Uses `success in {1, -1}` with `success == 1` as correct, plots `|ILD| = 1/2/4/8/16` curves with SEM on the left and aggregate accuracy on the right, and prints per-bin `ABL` fractions below the `|ILD|` panel plus per-bin `|ILD|` fractions below the aggregate panel.**
- `plot_valid_accuracy_vs_stim_quantile_bins_compare_abl_per_abs_ild.py` - **NEW: Weber-law-style valid-trial accuracy comparison. Builds separate `pd.qcut` stimulus bins within each `( |ILD|, ABL )` group, lays out a `1 x 5` panel figure over `|ILD| = 1/2/4/8/16`, and overlays `ABL = 20/40/60` accuracy-vs-stim curves with SEM in each panel. Text below each subplot prints the per-quantile trial counts for all three ABLs.**
- `plot_stim_distribution_early_vs_late_filtered_vs_unfiltered.py` - **NEW:** Diagnostic script for checking whether proactive truncation changes the pooled `intended_fix` segmentation. Rebuilds the pooled RTD/CDF plot trial set with and without removing `abort_event == 3` trials having `TotalFixTime < trunc_t`, computes separate 2-bin `pd.qcut` segment edges for both cases, and saves multiple `intended_fix` diagnostics under `stim_distribution_early_vs_late_filtered_vs_unfiltered/`: early/late overlays, removed-trial distribution, overall before-vs-after distribution with cutoff lines, overall CDF, filtered-minus-unfiltered count difference, and per-bin removed fraction.
- `check_truncation_removal_counts.py` - **NEW: prints batch-wise and batch+animal summaries of how many `valid + abort3` rows are removed by the proactive truncation thresholds (default `0.3 s`, `LED34_even = 0.15 s`) using the merged batch CSVs.**
- `plot_model_rtd_by_abl_stim_segments_all_animals_avg_params.py` - UPDATED: Builds pooled model RTD summaries across all animals that have both `vbmc_aborts_results` and `vbmc_norm_tied_results`, but first averages fitted parameters across animals. Supports pooled-trial quantile or fixed `intended_fix` segments, `TRIAL_POOL_MODE = "valid"` or `"valid_plus_abort3"`, `MODEL_DENSITY_MODE = "raw"` or `"valid_conditioned"`, and proactive-truncation removal of pre-stim abort mass before averaging the theory. Produces RTD and CDF plots with segment stim-ranges and density areas in the titles.
- `sim_validate_rtd_by_abl_stim_segments_all_animals_avg_params.py` - **NEW: Simulation-based validation of the theoretical stim-segment RTD/CDF plots. Uses the same data infrastructure (candidate discovery, pooled valid-df, quantile segmentation, aggregate mean-of-means params) as `plot_model_rtd_by_abl_stim_segments_all_animals_avg_params.py`, but replaces the analytical likelihood with 1M Monte Carlo trials per segment via `psiam_tied_data_gen_wrapper_rate_norm_fn`. Samples (ABL, ILD, t_stim) jointly from pooled empirical data within each segment to match distributions. Outputs 1×3 RTD density histograms and 1×3 empirical CDF plots (early vs late segment, by ABL).**
- `plot_theory_vs_data_rtd_by_abl_stim_segments.py` - UPDATED: 3×3 theory-vs-data RTD comparison grid. Rows 1–2 show early/late `intended_fix` quantile segments; row 3 shows all-segments pooled. Each column is one ABL (20/40/60). `THEORY_DATA_MODE` selects `"avg_of_rtds"` or `"avg_of_params"`, `MODEL_DENSITY_MODE` selects `"raw"` or `"valid_conditioned"`, and the script now supports pooled `abort3+abort4` trial sets, uses the wider RT window only for building segment edges, and reports `0..1 s` density area summaries instead of valid-fraction annotations. Outputs PDF+PNG under `theory_vs_data_rtd_stim_segments/`.**
- `plot_abort_dist_early_vs_late_stim.py` - **NEW: compares proactive abort distributions in `TotalFixTime` coordinates for early-stim, late-stim, and overall trial pools. Uses animals with both abort and tied fits, removes early abort3 trials via batch-specific truncation, and overlays per-animal-averaged data histograms (scaled by abort fraction) against proactive theory. Saves PDF+PNG under `abort_dist_early_vs_late_stim/`.**
- `plot_model_rtd_by_abl_stim_segments_all_animals_avg_animal_rtds.py` - UPDATED: Parallel per-animal version of the previous model RTD plot. Loads one animal's params at a time, builds that animal's RTDs from Monte Carlo `intended_fix` samples and its own empirical ILD weights, applies batch-specific proactive truncation before averaging, then averages the finished RTDs across animals with equal weight. Uses global segment edges so the result stays directly comparable to the pooled-data quantile plots, now supports pooled `abort3+abort4` trial sets, and stores per-ABL `0..1 s` density areas in the overlay payload for downstream comparisons.
- `plot_compare_data_vs_model_rtd_by_abl_stim_segments.py` - UPDATED: loads the saved data/model RTD overlay payload PKLs, requires exact ABL/segment-edge compatibility, and plots a 1x3 `ABL = 20/40/60` comparison with solid data curves and dashed model curves using the same early/late colors. Titles now report the `0..1 s` area for each segment in both data and model.
- `plot_model_rtd_by_abl_stim_segments_all_animals_avg_params_truncated_proactive.py` - Average-params model RTD plot with an additional proactive truncation step. Before building the valid-conditioned RTD, it removes proactive abort mass occurring before stimulus onset when absolute fixation time lies in `[0, 0.3)` s, renormalizes the proactive density, and then computes the pooled segment RTDs.
- `plot_model_rtd_by_abl_stim_segments_all_animals_avg_animal_rtds_truncated_proactive.py` - Per-animal RTD-averaging companion to the previous script. Applies the same proactive truncation per sampled `t_stim` and per animal before building each animal's RTD, then averages the finished RTDs across animals.
- `plot_valid_rtd_by_abl_stim_segments_all_animals_avg_animal_rtds.py` - Data-side companion to the per-animal model RTD plot. Uses the same global `intended_fix` segments, computes RTD histograms per animal and per `ABL`, then averages the finished RTDs equally across animals while excluding missing `segment x ABL` slices with `NaN` rather than zero-filling.
- `investigate_valid_rtd_stim_segment_mismatch.py` - Diagnostic script for the early-vs-late RTD mismatch. On a chosen batch (default LED7), compares data against multiple model constructions, including the old raw-current RTD and the corrected valid-conditioned RTD, prints valid-rate summaries across fixation bins, and saves comparison figures under `stim_segment_mismatch_diagnostics/`.
- `plot_model_rtd_by_abl_stim_examples_single_animal.py` - **NEW: loads one animal's `vbmc_aborts_results` and `vbmc_norm_tied_results`, evaluates deterministic model RTD curves for two fixed `t_stim` examples, overlays `ABL = 20, 40, 60`, mixes signed ILDs using empirical within-segment weights, and saves figures under `model_rtd_stim_examples_single_animal/`.**
- `plot_vbmc_param_summary_per_animal_NO_TRUNC_with_lapse.py` - **NEW: generates summary error-bar plots (mean ± 95% CI) of VBMC parameters per animal + aggregate for the NO_TRUNC_with_lapse model.**
- `tachometric_and_abort_fraction_analysis.py` - **DELETED: functionality merged into other analysis scripts**
- `vbmc_compare_LED_fit_average_animals.py` - UPDATED: now supports both mean and median aggregation methods via AGG_METHOD parameter, changed animal list to [93, 98, 99, 100], updated bin width to 0.005, outputs files with aggregation method in filename
- `vbmc_real_data_proactive_LED_fit_CORR_ID.py` - UPDATED: added LED ON trial upweighting (weight=10) in compute_trial_loglike function to emphasize LED ON trials during fitting
- `vbmc_refit_old_truncated_proactive_led7_93_more_evals.py` - Dedicated non-overwriting refit of the paper-era three-parameter LED-OFF proactive model for LED7/93. It reproduces the historical 300 ms truncation/censor likelihood on 10,891 rows, validates a vectorized objective against the scalar implementation, uses the corrected `t_A_aff = -0.5..0.1 s` bounds, and raises the VBMC evaluation cap to 10,000 while saving posterior samples, the variational posterior, convergence metadata, and an old-versus-refreshed summary.
- `replace_led7_93_proactive_vbmc_block_with_converged_refit.py` - Audited one-time promotion of the converged LED7/93 proactive refit into the canonical combined paper-fit pickle. It preserves a hash-checked copy of the former pickle, regenerates the canonical one-million-draw proactive sample arrays from the corrected VBMC posterior, atomically replaces only `vbmc_aborts_results`, verifies that all three downstream TIED blocks are unchanged, and records hashes and exact provenance in a JSON sidecar.
- `numpyro_proactive_led_step_jump_svi_utils.py` - JAX/NumPyro implementation of the five-parameter proactive LED step-jump model. Provides the exact PDF, stable inverse-Gaussian CDF terms, fixed Gauss-Legendre post-jump CDF integration, the unweighted bilateral LED-ON plus LED-OFF truncation/censor likelihood, matching trapezoidal priors, and a full-rank variational guide.
- `validate_numpyro_proactive_led_step_jump_likelihood.py` - Compares the JAX PDF, CDF, truncation-adjusted survival, and complete unweighted log likelihood against the NumPy/VBMC-reference equations for no-jump, upward-jump, and downward-jump cases. The current validation selects 64 quadrature nodes.
- `numpyro_svi_proactive_led_step_jump_single_animal.py` - Cell-wise single-animal SVI fit for `V_A_base`, `V_A_post_LED`, `theta_A`, `del_a_minus_del_LED`, and `del_m_plus_del_LED`. Uses exact trial-wise `t_LED` without binning, a clipped-Adam full-rank guide, 1,000-step convergence windows, patience 12, and restored-best posterior sampling.
- `run_numpyro_svi_proactive_led_step_jump_led7.py` - Resumable sequential LED7 runner for animals `92, 93, 98, 99, 100, 103`. Requires a passed likelihood-validation summary, skips matching completed fits, writes per-animal logs and a batch ledger, and supports `--only` for smoke tests or partial reruns.
- `plot_numpyro_svi_proactive_led_step_jump_led7_loss_grid.py` - Validates and plots the selected six-animal LED7 proactive step-jump SVI loss traces in a `2 x 3` grid. It uses the extended 150k audits for animals `92, 100, 103` and the original 50k runs for `93, 98, 99`; green lines mark restored-best checkpoints, red dashed lines mark final checked steps, and a provenance-bearing convergence summary CSV is exported.
- `plot_numpyro_svi_proactive_led_step_jump_rtwrtled_2x7.py` - Builds an FCT-style `2 x 7` RT-relative-to-LED diagnostic from the selected proactive step-jump SVI fits. Columns show six LED7 animals plus a pooled-trial column. Individual data use 20 ms regular and 10 ms zoomed bins; pooled data use 10 ms and 5 ms bins. Theory uses 5 ms resolution and 5,000 Monte Carlo draws of intact `(t_LED, t_stim)` rows, with the same paired timing draws used for LED ON and OFF. Data and model use the fitted 300 ms left truncation, abort-fraction density scaling, and 150k audit fits for animals `92, 100, 103`.
- `plot_numpyro_svi_proactive_led_step_jump_rtwrtled_2x7_coarse_individual_bins.py` - Replots the same saved data and theory without replacing the original figure. The six individual columns use coarser 40 ms regular and 20 ms zoomed data bins to reduce histogram noise; the pooled column remains at 10 ms and 5 ms, and the model curves are copied unchanged from the source payload. Red dotted lines and animal-title values mark the fitted drift-switch times; the pooled panel displays all six switch markers.
- `numpyro_svi_proactive_led_step_jump_aggregate_led7.py` - Fits one shared five-parameter proactive LED step-jump model to the pooled retained trials from all six LED7 animals. It keeps the animal-wise SVI likelihood conventions (bilateral LED ON, LED OFF, 300 ms left truncation, no lapses, and unweighted trial contributions), uses patience-12 restore-best stopping, and automatically extends a terminal-best 150k run to 250k.
- `plot_numpyro_svi_proactive_led_step_jump_aggregate_rtwrtled.py` - Builds a pooled `1 x 2` RT-relative-to-LED diagnostic from the shared aggregate posterior. It uses 10 ms and 5 ms data bins, 5 ms theory, 5,000 intact `(t_LED, t_stim)` timing-row draws, abort-fraction scaling, and explicit fitted-switch and 300 ms truncation markers.
- `numpyro_proactive_led_step_jump_no_trunc_exp_lapse_svi_utils.py` - Seven-parameter JAX/NumPyro companion matching the historical FCT model. It reuses the validated step-jump PDF/CDF, adds the exponential lapse density/survival mixture, preserves the old trapezoidal priors, and contains no 300 ms truncation denominator.
- `validate_numpyro_proactive_led_step_jump_no_trunc_exp_lapse_likelihood.py` - Validates the no-truncation lapse PDF, CDF, abort mixture, censored-survival mixture, finite gradients, and full LED7/93 log likelihood against independent NumPy/SciPy references. The exact FCT selection contains 16,486 LED7/93 rows, including 664 aborts below 300 ms.
- `numpyro_svi_proactive_led_step_jump_no_trunc_exp_lapse_single_animal.py` and `run_numpyro_svi_proactive_led_step_jump_no_trunc_exp_lapse_led7.py` - Cell-wise animal fit and resumable sequential runner for all six LED7 animals. They use every LED-ON row plus LED-OFF, full-rank SVI, patience-12 restore-best stopping, and continue the same SVI state from 150k to at most 250k when patience has not triggered.
- `plot_numpyro_svi_proactive_led_step_jump_no_trunc_exp_lapse_loss_grid.py` - Plots the six no-truncation lapse loss traces with restored-best and final-checked steps and writes a convergence summary CSV.
- `plot_numpyro_svi_proactive_led_step_jump_no_trunc_exp_lapse_rtwrtled_2x7.py` - Builds the no-truncation FCT-style `2 x 7` RT-relative-to-LED diagnostic. It uses posterior-mean seven-parameter fits, 5,000 intact timing-pair draws, raw lapse-mixture abort-rate scaling, and a pooled observed/trial-weighted model column without a 300 ms boundary.
- `numpyro_proactive_led_step_jump_npl_alpha_utils.py` - Combines the validated proactive step-jump PDF/CDF with the current NPL+alpha RT+choice race. LED OFF and LED ON have separate PDF, CDF, and valid-window likelihood entry points. The optional proactive lapse is a direct exponential-time random-choice trial mixture, and the valid likelihood is conditioned on `0 <= RTwrtStim <= 1 s` without a 300 ms abort truncation.
- `simulate_proactive_led_step_jump_npl_alpha.py` - Independent Monte Carlo simulator for the same model. Ordinary trials simulate the proactive and evidence accumulators, including the `del_go` evidence readout after a proactive win; lapse trials bypass both accumulators and make a random choice at an exponential fixation time.
- `validate_proactive_led_step_jump_npl_alpha_likelihood.py` - Validates the separate LED-OFF and LED-ON likelihoods against 100,000-trial simulations for a no-jump control, an upward-jump model, and the LED7/93 posterior-derived case. Every case uses the same 128 stratified LED7/93 timing rows, preserving paired LED-ON `t_stim`/`t_LED` values. It tests both unconditioned responses and the `0..1 s` valid window, with and without proactive lapses, and saves four separate RTD figures plus numerical checks and reusable data.
- `plot_proactive_led_step_jump_npl_alpha_lapse_choice_collapsed_rtd.py` - Collapses the validated `+1` and `-1` likelihood components into the full abort-plus-valid RTD over `-1..2 s`. It keeps LED OFF and LED ON in separate figures, omits the downward-jump numerical stress test, retains the no-jump case only as a control, and records simulation-versus-likelihood mass checks without display-window renormalization.
- `numpyro_proactive_led_step_jump_npl_alpha_valid_svi_utils.py` - Reusable NumPyro model wrapper for fitting the seven shared NPL+alpha parameters plus condition-wise `t_E_aff` while holding the validated proactive step-jump and exponential-lapse parameters fixed. LED-OFF and bilateral LED-ON valid-trial likelihoods remain separate and are conditioned on the requested RT window.
- `numpyro_svi_proactive_led_step_jump_npl_alpha_valid_led7_98_pilot.py` - LED7/98 pilot that independently fits the 7,791 LED-OFF and 1,170 bilateral LED-ON successful trials in `0 <= RTwrtStim <= 1 s`. It initializes the 37-dimensional full-rank guide from the previous LED7/98 NPL+alpha posterior, fixes proactive/lapse values at the accepted all-LED-ON abort-fit posterior means, and saves patience-12 restore-best fit artifacts with exact provenance.
- `plot_led7_98_proactive_lapse_npl_alpha_valid_pilot_diagnostics.py` - Builds the paired convergence plot, seven-global-plus-delay posterior comparison, and separate `3 x 5` choice-collapsed full-RTD grids for the LED7/98 pilot. The parameter legend explicitly distinguishes the previous 300 ms proactive-truncation/no-lapse fit from the new no-proactive-truncation+lapse fits. OFF RTD data include abort events 3 and 4 plus successful trials; bilateral-ON data retain event 3 plus successful trials and exclude event 4. Both preserve displayed probability mass and average model likelihoods over exact trial timings and signed-condition frequencies.
- `numpyro_svi_proactive_led_step_jump_npl_alpha_valid_led7_aggregate.py` - Pools all six LED7 animals without animal reweighting and independently fits 52,799 LED-OFF and 8,138 bilateral LED-ON valid RT+choice trials. Each category runs warm and deliberately displaced 37-parameter full-rank SVI branches, fixes proactive/lapse parameters from the accepted aggregate abort fit, applies patience-12 restore-best stopping, and records the accepted branch using common ELBO and posterior-mean likelihood criteria.
- `plot_numpyro_svi_proactive_led_step_jump_npl_alpha_valid_led7_aggregate_diagnostics.py` - Reads the explicit warm/displaced selection records, overlays both convergence histories, and builds separate pooled `3 x 5` OFF and bilateral-ON RTD grids at 1 ms model resolution. OFF empirical histograms include abort events 3 and 4 plus successful trials; bilateral-ON histograms retain event 3 plus successful trials and exclude event 4. Both retain their displayed probability mass over `-1..1 s`.
- `plot_led7_super_animal_valid_npl_alpha_displaced_convergence.py` - Adds a focused `1 x 2` convergence view using only the deliberately displaced LED-OFF and bilateral LED-ON branches. It marks each restored-best and final-checked step and exports a compact provenance summary without changing the warm/displaced selection records or RTD diagnostics.
- `compare_led7_super_animal_off_vs_on_npl_alpha_params.py` - Compares the explicitly accepted pooled LED-OFF and bilateral-ON NPL+alpha posteriors. Seven global-parameter panels show posterior means and 95% intervals, while the eighth panel aligns all 30 condition delays by `(ABL, ILD)` and plots OFF versus ON means with two-axis 95% intervals, an equality line, and delay correlation/RMSE summaries.
- `numpyro_svi_proactive_led_step_jump_no_trunc_exp_lapse_aggregate_led7.py` - Fits one shared seven-parameter proactive step-jump plus exponential-lapse model to all 94,433 selected LED7 trials. It pools trials without animal reweighting, retains all 3,081 aborts below 300 ms, runs at least 150k SVI steps, and requires 12 checked windows after the absolute-best checkpoint before accepting restored-best posterior samples; a late best extends the same optimizer state to at most 250k.
- `plot_numpyro_svi_proactive_led_step_jump_no_trunc_exp_lapse_rtwrtled_2x8.py` - Extends the FCT-style diagnostic to six animal columns plus two pooled-data comparisons. Column 7 uses a trial-count-weighted mixture of selected animal-wise posterior means, while column 8 uses the single shared aggregate posterior mean. The script selects 150k audits for animals `92`, `99`, `100`, and `103`, retains the accepted 50k fits for `93` and `98`, and saves a provenance-bearing reusable payload.
- `compare_fct_and_aggregate_svi_red_theory.py` - Diagnostic used to isolate the former 300 ms kink by comparing the historical no-truncation FCT LED-ON curve against raw and truncation-conditioned predictions from the shared five-parameter aggregate SVI fit.
- `check_proactive_LED_with_identifiable_params.py` - Sanity-check script comparing equivalent simulator parameterizations for identifiable delays
- `bads_real_data_proactive_LED_fit_CORR_ID.py` - Same as vbmc_real_data_proactive_LED_fit_CORR_ID.py but uses BADS (Bayesian Adaptive Direct Search) for point-estimate optimization instead of VBMC. Minimizes negative log-likelihood directly (no priors). Outputs best-fit params, RTD comparison, and RT-wrt-LED diagnostics
- `vbmc_compare_LED_fit_average_animals.py` - Averages RT-wrt-LED abort-rate scaled histograms across all animals. Loads per-animal VBMC posteriors (CORR_ID), computes data and simulated scaled histograms per animal, then averages and plots
- `sum_invariant_delays_proactive_rtds.py` - Utilities/experiments for invariances in proactive RTDs with combined delay parameters
- `test_stim_timing_distributions.py` - Debug script to verify if t_stim and t_LED distributions differ between LED ON/OFF trials (they do not)
- `post_LED_censor_utils.py` - Utility functions for post-LED censoring (cum_A_t_fn etc.)
- `../fit_animal_by_animal/proactive_plus_lapse_plus_reactive_uitls.py` - Utility functions for proactive + lapse + reactive model fitting with both post-censoring at 150ms after stimulus, right-truncation helpers for the LED-OFF fit variants, and a choice-collapsed RT likelihood used by the no-choice ABL-delay fits.
- `fit_only_LED_off_with_LED_ON_fits.py` - **NEW: Fits normalized TIED parameters on LED-OFF data using proactive+lapse parameters loaded from proactive VP pickles. Supports both aggregate and animal-wise fitting modes with post-censoring at 150ms after stimulus. Includes corner plot generation and comprehensive result saving.**
- `fit_only_LED_off_with_LED_ON_fit_diagnostics.py` - **NEW: Aggregate diagnostics for the censoring-based LED-OFF tied fit. Rebuilds the filtered LED-OFF dataset, loads the aggregate fit pickle, Monte Carlo-averages the raw RT-wrt-stim theory, and compares it to empirical data with the 150ms censor threshold marked.**
- `fit_only_LED_off_with_LED_ON_fits_truncate_NOT_censor.py` - **NEW: Right-truncated variant of the LED-OFF tied fit. Keeps only trials with `0 < RTwrtStim <= 1.0 s` during fitting and renormalizes retained trial likelihoods by the mass in `(t_stim, t_stim + 1.0 s]`.**
- `fit_only_LED_off_with_LED_ON_fit_diagnostics_truncate_not_censor.py` - **NEW: Aggregate diagnostics for the truncation-based LED-OFF tied fit. Loads the truncation-fit pickle, plots the raw full RT-wrt-stim comparison against all filtered trials, and also saves a second plot clipped to `0..1.0 s` with the clipped theory renormalized to area 1.**
- `fit_only_LED_off_linear_delay_no_choice.py` - **NEW: Aggregate/animal-wise right-truncated LED-OFF tied fit with a linear evidence-delay surface `bias + abl_coeff * ABL + ild_coeff * |ILD|`. Uses the choice-collapsed RT density, enforces nonnegative delay over observed `(ABL, |ILD|)` conditions with a finite likelihood penalty, and saves posterior summaries including the derived delay surface.**
- `fit_only_LED_off_linear_delay_WITH_choice.py` - **NEW: Choice-aware twin of the linear-delay LED-OFF fit. Keeps the same 9 tied parameters and linear delay rule, but uses the observed trial choice in the bound-specific right-truncated likelihood and writes results under the `linear_delay_with_choice` artifact family.**
- `linear_delay_no_choice_diagnostics_utils.py` - **NEW: Shared helper module for the linear-delay diagnostics workflow. Holds the reusable theory-curve, MC-averaging, truncation, and quantile-computation utilities used by the FAST diagnostics script.**
- `fit_only_LED_off_fit_diagnostics_truncate_not_censor_linear_delay_no_choice_FAST.py` - **NEW: FAST aggregate diagnostics for the linear-delay no-choice fit. Rebuilds the LED-OFF dataset, computes vectorized RTD theory, saves raw/truncated/per-ABL/per-condition diagnostics, plots the fitted delay surface, adds a `1 x 3` quantile-vs-`|ILD|` diagnostic with dense model curves and red empirical quantile markers, and now also saves two short-latency overlay summaries: `1 x 5` panels by `|ILD|` with all ABLs overlaid and `1 x 3` panels by ABL with all `|ILD|` conditions overlaid (`xlim = 115 ms`).**
- `compare_ABL_delay_115ms_vs_linear_delay_no_choice.py` - **NEW: compares the 115 ms no-choice ABL-delay fit against the aggregate no-choice linear-delay fit. Rebuilds the LED-OFF diagnostics dataset, reconstructs posterior-mean theory curves for both fits, and overlays data plus both models in per-ABL RTD panels with a 115 ms display window.**
- `fit_only_LED_off_fit_diagnostics_truncate_not_censor_linear_delay_WITH_choice_FAST.py` - **NEW: Choice-fit twin of the FAST linear-delay diagnostics script. Uses the same current diagnostics workflow and helper module as the no-choice version, but reads the `linear_delay_with_choice` aggregate results pickle and writes diagnostics under the corresponding choice-fit artifact directory.**
- `fit_only_LED_off_log2_abs_ild_delay_no_choice.py` - **NEW: No-choice LED-OFF tied fit variant that replaces the linear `|ILD|` delay term with `log2(|ILD|)`. Keeps the same 9-parameter right-truncated fit structure, uses `delay = bias + abl_coeff * ABL + log2_ild_coeff * log2(|ILD|)`, rescales the `|ILD|` coefficient prior to match the transformed covariate, and writes results under the `log2_abs_ild_delay_no_choice` artifact family.**
- `fit_only_LED_off_interaction_delay_no_choice.py` - **NEW: No-choice LED-OFF tied fit variant that keeps the linear main effects in `ABL` and `|ILD|` and adds a scaled interaction term. Uses `delay = bias + abl_coeff * ABL + ild_coeff * |ILD| + interaction_coeff * ((ABL - 40) / 20) * (|ILD| / 16)`, fits 10 tied parameters under the same right-truncated RT-only likelihood, enforces nonnegative delay over observed fit conditions, and writes results under the `interaction_delay_no_choice` artifact family.**
- `fit_only_LED_off_with_LED_ON_fits_truncate_NOT_censor_ABL_delay.py` - **NEW: Right-truncated LED-OFF tied fit with ABL-specific evidence-afferent delays. Uses proactive+lapse parameters loaded from the no-trunc LED-ON fits, keeps only trials with `0 < RTwrtStim <= truncate_rt_wrt_stim_s`, fits separate `t_E_aff` values for ABL 20/40/60, and saves VBMC objects, corner plots, posterior samples, and trial-count summaries.**
- `fit_only_LED_off_with_LED_ON_fit_diagnostics_truncate_not_censor_ABL_delay.py` - **NEW: Aggregate diagnostics for the ABL-delay truncation fit. Loads the aggregate ABL-delay truncation-fit pickle, rebuilds the LED-OFF dataset, Monte Carlo-averages the RT-wrt-stim theory with ABL-specific `t_E_aff`, and saves raw plus truncated/renormalized comparison plots including per-ABL panels.**
- `fit_only_LED_off_with_LED_ON_fits_truncate_NOT_censor_ABL_delay_no_choice.py` - **NEW: No-choice companion to the ABL-delay truncation fit. Uses the choice-collapsed RT density `pdf(up)+pdf(down)`, supports aggregate-only fixed per-ABL trial-count sampling after truncation, and now writes run-tagged artifact names that encode truncation plus fixed-trial mode.**
- `fit_only_LED_off_with_LED_ON_fit_diagnostics_truncate_not_censor_ABL_delay_no_choice.py` - **NEW: Aggregate diagnostics for the no-choice ABL-delay truncation fit. Loads the run-tagged no-choice result pickle, verifies the requested run tag matches the saved fit config, and saves raw/truncated/per-ABL/publication diagnostics with matching run-tagged filenames. Supports loading matched-stim-reference fit pickles when `fit_results_variant = "match_ref_stim"` and `match_stim_distribution = True`; otherwise it falls back to the standard truncation-fit pickle while leaving the empirical diagnostics dataset unchanged.**
- `fit_only_LED_off_with_LED_ON_fits_truncate_NOT_censor_ABL_delay_no_choice_match_ref_stim.py` - **NEW: Aggregate-only no-choice ABL-delay truncation fit that uses a larger target truncation (for example `130 ms`) but subsamples that candidate pool so its `intended_fix` distribution matches a reference truncation (for example `115 ms`) within each `(ABL, ILD)` stratum. Saves run-tagged VBMC/results/corner artifacts plus a 2x2 pre-vs-post stimulus-matching diagnostic plot and a detailed matching audit in the results pickle.**
- `plot_no_choice_posterior_compare_two_fits.py` - **NEW: Loads the saved 130 ms no-choice aggregate result pickles for the all-valid and fixed-trial-count runs, overlays all 9 posterior marginals in a 3x3 grid, marks the 2.5/97.5 percentile lines, and reports posterior-width changes directly in the panel titles plus a console summary table.**
- `plot_led_off_tachometric_by_abl_two_truncations.py` - **NEW: Rebuilds the aggregate LED-OFF valid-trial dataset with the same filters as the ABL-delay truncation fit, then plots ILD-collapsed tachometric curves (`accuracy` vs `RTwrtStim`) for `ABL = 20, 40, 60` under two right-truncation windows (`100 ms` and `130 ms`) in one overlaid figure. Saves PDF+PNG under `led_off_tachometric_by_abl_two_truncations/`.**
- `plot_led_off_signed_rtd_by_abl_two_truncations.py` - **NEW: Rebuilds the same aggregate LED-OFF valid-trial dataset, splits valid trials into up (`response_poke == 3`) and down (`response_poke == 2`) responses, and plots a `1 x 3` signed RTD histogram figure by ABL for `100 ms` vs `130 ms` truncations. Up densities are positive, down densities are negative, and each `(ABL, truncation)` pair is normalized so up area plus absolute down area equals 1. Saves PDF+PNG under `led_off_signed_rtd_by_abl_two_truncations/`.**
- `compare_fit_only_LED_off_with_LED_ON_censor_vs_truncate.py` - **NEW: Loads the censoring and truncation fit result pickles side-by-side, compares posterior/proactive parameter summaries, rebuilds the training datasets for both fits, and cross-evaluates each posterior mean under the censor, truncation, and raw no-censor likelihoods.**
- `fit_only_LED_off_with_LED_ON_fit_diagnostics_truncate_not_censor_ABL_delay_no_choice_FAST.py` - **NEW: Vectorized (fast) version of the no-choice ABL-delay diagnostics script. Replaces the scalar per-time-point inner loop with vectorized calls to `_vec` functions from `time_vary_norm_utils.py` (~260x speedup). Data histograms use un-normalized scaling (`counts / (n_total_abl × bin_width)`). Produces raw, truncated, per-ABL, and publication-style plots with run-tagged filenames, and supports loading matched-stim-reference fit pickles only when `fit_results_variant = "match_ref_stim"` and `match_stim_distribution = True`; otherwise it falls back to the standard truncation-fit pickle without changing the empirical diagnostics dataset.**
- `compare_trunc_115_vs_130_allvalid.py` - **NEW: Loads the saved diagnostics payloads for the 115ms and 130ms fixN=OFF fits and overlays both model curves against 130ms-truncated data in a 1×4 layout (ABL 20, 40, 60, combined). Legends include per-ABL fitted delays (δ) from each fit.**
- `plot_no_choice_posterior_compare_four_fits.py` - **NEW: Multi-fit no-choice posterior comparison script. Loads a configurable set of aggregate result pickles (currently including `100/115/130/145 ms` all-valid fits plus the `115/130 ms` fixed-trial-count fits), overlays all 9 posterior marginals in a 3×3 grid, and prints posterior-width summaries across all selected conditions.**
- `plot_no_choice_posterior_compare_two_fits_match_ref_vs_115ms.py` - **NEW: Two-fit posterior comparison for the matched-truncation question. Overlays all 9 posterior marginals for `115 ms allvalid` versus `130 ms match-ref-stim`, reports the saved fit trial counts in the legend, and includes the matched-fit KS audit summary in the figure title/console output.**
- `plot_no_choice_posterior_compare_two_fits_115ms_vs_130ms_allvalid.py` - **NEW: Two-fit posterior comparison for the standard all-valid truncation change. Overlays all 9 posterior marginals for `115 ms allvalid` versus `130 ms allvalid` and reports the actual per-fit trial counts so the unequal-size comparison is explicit.**
- `plot_led_off_intended_fix_two_truncations.py` - **NEW: Plots the `intended_fix` (stimulus timing) distribution of LED-OFF trials, with vertical lines marking the 115ms and 130ms truncation thresholds and trial-count breakdowns.**
- `check_data_scaling_per_abl.py` - **NEW: Investigation script comparing three data histogram scaling options (density=True, counts/n_total/binw, counts/n_abl/binw) against un-normalized theory area per ABL. Confirmed `counts/(n_total_abl × bin_width)` matches theory scale.**
- `test_vectorized_per_abl.py` - **NEW: Validation script comparing scalar vs vectorized theory curve implementations. Verifies all 15 test cases (5 trials × 3 ABLs) pass with max absolute error ~1e-13 and reports speedup.**
- `LED_OFF_TRUNCATE_choice_only_GUIDE.md` - **NEW: Quick-reference guide explaining the fitting/diagnostics pipeline, the 4 fit cases (115ms/130ms × fixN ON/OFF), data normalization, vectorization, and how to switch between cases.**
- `stim_distribution_matching_algorithm.md` - **NEW: Short algorithm note for the matched-reference truncation fit. Explains why `intended_fix` matching is needed, how `(ABL, ILD)`-stratified quantile-bin subsampling works, what `quantile_bins` and `min_trials_per_bin` do, what is guaranteed exactly, how the KS audit should be interpreted, and what happens in fallback/error cases.**
- `estimate_delays_from_RTDs_rise.py` - **NEW: Rebuilds the aggregate LED-OFF LED7 dataset from `out_LED.csv`, plots truncated RTDs in a `1 x 3` ABL layout, and estimates rough rise times from the data-only RTD using a short-window slope/rise heuristic.**
- `estimate_delays_from_RTDs_rise_by_abl_abs_ild.py` - **NEW: Extends the data-only rise-time diagnostic to a `3 x 5` `(ABL, |ILD|)` grid with per-panel truncated RTDs and heuristic rise estimates.**
- `estimate_delays_from_RTDs_rise_by_abl_abs_ild_kde_overlay.py` - **NEW: Adds Epanechnikov-KDE smoothing on the same `3 x 5` `(ABL, |ILD|)` RTD grid so raw histograms and smoothed RTDs can be compared directly before choosing an onset rule.**
- `estimate_delays_from_RTDs_rise_by_abl_abs_ild_kde_slope.py` - **NEW: Main slope-based delay-analysis script for the LED-OFF aggregate dataset. Builds KDE-smoothed RTDs, rolling slope estimates, `10%`-of-peak and baseline-plus-`Nσ` onset lines, exports the chosen baseline-significant delay table CSV, and saves both standard and transposed slope figures plus ABL-overlay RTD summaries.**
- `manual_pick_delays_from_RTDs_by_abl_abs_ild.py` - **NEW: Local interactive Matplotlib picker for manual delay annotation. Shows one `(ABL, |ILD|)` condition at a time with RTD/KDE and slope panels, supports click/drag plus keyboard nudging, and autosaves picks to CSV for downstream overlays.**
- `fit_delay_surface_from_5sigma_csv.py` - **NEW: Loads the exported baseline-significant delay matrix CSV, reshapes it to `(ABL, |ILD|, delay)` points, fits linear and quadratic delay surfaces with NumPy least squares, compares them by `R^2`, and saves matrix/scatter diagnostics plus slice plots (`delay vs |ILD|` by ABL and `delay vs ABL` by `|ILD|`).**
- `fit_delay_surface_from_5sigma_csv_ild2.py` - **NEW: Companion delay-surface script that keeps the same 5σ CSV analysis but compares the linear model against the restricted `bias + c1*ABL + c2*|ILD| + c3*ILD^2` surface and writes separate `_ild2` outputs.**
- `save_LED7_expert_data.py` - **NEW: One-shot LED7 expert-dataset export script. Rebuilds the standard LED-OFF expert trial table from `out_LED.csv`, adds derived columns (`batch_name`, `RTwrtStim`, `t_LED`, `abs_ILD`), reconstructs missing `response_poke` on successful rows, keeps LED7/session-type-7/training-level-16/repeat-trial `[0, 2]` rows plus valid and `abort_event in {3, 4}` trials, and writes `LED7_expert_data.csv` at the repo root.**
- `plot_led7_rtd_by_abl_1x3.py` - **NEW: Rebuilds LED7 LED-OFF valid/abort rows from `out_LED.csv`, then plots RTwrtStim histograms by ABL in a 1x3 layout over the short-latency window.**
- `plot_led7_rtd_by_abl_1x3_expert_csv.py` - **NEW: Same LED7 1x3 RTD diagnostic using `LED7_expert_data.csv`, with an optional raw-CSV consistency check against `out_LED.csv`.**
- `plot_led7_control_opto_rtd_by_abl_1x3.py` - **NEW: Compares LED7 control and opto conditions from the expert CSV/raw data in ABL-wise short-latency RTD panels.**
- `plot_led7_npl_alpha_patience12_fit_aligned_valid_rtds.py` - **NEW: Rebuilds LED7 valid-trial RTDs from the exact `0..1 s` patience-12 NPL+alpha condition-delay fit rows, evaluates trial-aligned proactive+reactive theory, and saves complete plus conditional right-truncation comparisons with pooled and equal-animal averaging.**
- `plot_led7_npl_alpha_patience12_fit_aligned_quantiles.py` - **NEW: Compares LED7 data and patience-12 NPL+alpha RT quantiles by ABL and `|ILD|`, averaging signed conditions within animal before across-animal mean and SEM summaries.**
- `plot_led7_npl_alpha_patience12_fit_aligned_rtds_by_abl_abs_ild.py` - **NEW: Produces equal-animal LED7 data/model RTD grids by `ABL x |ILD|`, ABL/ILD marginal and grand-average panels, short-latency x-limit variants, a renormalized 120 ms right-truncation view, and the fitted `t_E_aff` summary by signed ILD and ABL.**
- `find_led7_stimulus_modulation_onset_by_animal.py` - **NEW: Estimates animal-wise LED7 stimulus-modulation onset by comparing early valid-trial RT distributions for `|ILD|=1` and `|ILD|=16`, with consecutive-significance and minimum-trial safeguards plus bootstrap and choice diagnostics.**
- `plot_led7_fig2a_fig2d_by_animal.py` - **NEW: Generates paper Fig. 2a/2d-style LED7 animal grids for RT CDFs by `|ILD|` and tachometric accuracy, including the common 100 ms reference cutoff.**
- `plot_led7_npl_alpha_reactive_ge100ms_fit_aligned_rtds.py` - **NEW: Compares data and reactive-only NPL+alpha RTDs conditioned on `100 ms <= RTwrtStim < 1 s`, with each signed condition normalized before equal-condition and equal-animal averaging.**
- `plot_led7_npl_alpha_reactive_ge100ms_t_E_aff_vs_ild_by_abl.py` - **NEW: Summarizes reactive-only LED7 posterior-mean condition delays as `t_E_aff` versus signed ILD, with ABL colors and across-animal SEM error bars.**
- `plot_led7_npl_alpha_rt_only_fit_aligned_rtds.py` - **NEW: Recreates the equal-animal `ABL x |ILD|` RTD grid for the completed proactive+reactive and reactive-only RT-only NPL+alpha fits. Data use only the exact successful `0 <= RTwrtStim < 1 s` fitting rows; models use the matching choice-collapsed likelihood, posterior-mean parameters, condition delays, 1 ms theory, and trial-wise `intended_fix` averaging for the proactive+reactive process.**
- `plot_led7_npl_alpha_rt_only_t_E_aff_vs_ild_by_abl.py` - **NEW: Plots matched condition-delay summaries for the completed proactive+reactive and reactive-only RT-only fits. Each point is the across-animal mean of six posterior-mean `t_E_aff` values, error bars are SEM across animals, and ABL 20/40/60 use the standard blue/orange/green colors.**
- `plot_led7_npl_alpha_patience12_t_E_aff_posterior_histograms.py` - **NEW: Displays the direct paper-source patience-12 37-parameter NPL+alpha condition-delay posterior as a `10 x 6` grid of raw 1 ms histograms. Rows are signed ILDs, columns are the six LED7 animals, and every panel overlays the 10,000-sample `t_E_aff` marginals for ABL 20/40/60. It also saves detailed and three-row summary tables for the requested uncorrected two-sample KS classifications (`p < 0.05`) across the 60 animal-by-ILD cases for ABL pairs 20/40, 40/60, and 20/60.**
- `plot_led7_s9_fixation_abort_distributions.py` - **NEW: Builds a `4 x 9` fixation-abort grid from the MAT-derived `raw_data/LED7_s9.csv`: eight animals plus a trial-pooled aggregate. Rows show LED-OFF and LED-ON fixation-time histograms, the two conditions aligned directly to the raw fixation-referenced `LED_onset_time`, and an identical `-0.1..0.5 s` LED-aligned zoom. All panels use 20 ms unfilled-step histograms scaled so area equals finite plotted aborts divided by every trial in that LED condition.**
- `plot_led7_s9_led_on_abort_onset_vs_intended_fix.py` - **NEW: Plots raw `LED_onset_time` against `intended_fix` for every LED-ON fixation abort in a `2 x 4` animal grid, with the identity line and only the sample size annotated below it.**
- `plot_led7_s9_led_on_abort_onset_distribution.py` - **NEW: Pools all animals and plots the raw-count distribution of `LED_onset_time` for LED-ON fixation aborts in 20 ms bins.**
- `plot_led7_s9_led_on_abort_intended_minus_onset_distribution.py` - **NEW: Pools all animals and plots the raw-count distribution of `intended_fix - LED_onset_time` for LED-ON fixation aborts in 20 ms bins.**
- `plot_led7_s9_fixation_abort_two_alignments.py` - **NEW: Builds a `2 x 9` LED-OFF/ON fixation-abort grid comparing `timed_fix - LED_onset_time` with `timed_fix - (intended_fix - LED_onset_time)`. Histograms use 20 ms bins, a displayed range of `-1..1 s`, all eligible condition trials as denominators, and a trial-pooled aggregate.**
- `plot_led7_s9_fixation_abort_two_alignments_by_session_half.py` - **NEW: Repeats the two-alignment `2 x 9` analysis separately for each animal's first and second halves of training-level-16 sessions. Odd session counts assign the midpoint session to the first half, and each aggregate pools trials from the matching within-animal halves.**
- `plot_led7_led8_four_batch_timing_abort_comparison.py` - **NEW: Builds the trial-pooled `4 x 5` timing and fixation-abort comparison for LED7 session types 7/8/9 and LED8 session type 8. It applies training-level-16 and repeat-trial filtering, uses all LED-ON rows for timing densities, and scales event-3 OFF/ON abort histograms by their full condition-trial denominators.**
- `plot_led7_s7_s8_all_vs_s9_onset_floor_4x5.py` - **NEW: Compares all eligible LED7 session-type-7/8 trials with a symmetric condition-wise filter removing derived LED onsets below the session-type-9 100 ms floor. Both OFF and ON denominators are recomputed after filtering before the event-3 abort distributions are normalized.**
- `plot_led7_led8_four_batch_timing_abort_psychometric_chronometric.py` - **NEW: Builds a trial-pooled `4 x 8` comparison for LED7 session types 7/8/9 and LED8 session type 8 after training-level-16 and repeat-trial filtering. Panels show LED-ON timing distributions, condition-normalized event-3 abort timing, valid-trial psychometrics, correct-choice chronometrics, and all-valid-trial chronometrics. The chronometric panels use derived `RTwrtStim` in `0..1 s`, show pooled means without error bars, and share a `0.10..0.35 s` y-range.**
- `save_LED8_session8_training16_repeat_filtered.py` - **NEW: MATLAB-backed exporter for LED8 session-type-8/training-16/repeat `[0, 2]` rows. Adds derived `RTwrtStim`, `t_LED`, `LED_time_wrt_fix`, and `abs_ILD` columns, keeps valid plus selected abort trials, removes LED-on rows after the assumed light window, and saves `LED8_session8_training16_repeat_filtered.csv`.**
- `led8_session8_led_off_rtds.py` - **NEW: Data-only LED8 session-type-8 LED-off RTD plot. Filters valid-choice rows with `0 <= RTwrtStim <= 1`, keeps LED-off trials, and saves a 3x5 `ABL x abs_ILD` histogram grid.**
- `plot_led8_abort_rate_rt_wrt_led_onset.py` - **NEW: Builds animal-wise plus aggregate LED8 abort-rate-scaled histograms of abort timing relative to LED onset for LED ON versus LED OFF trials.**
- `led8_session8_led_off_rtds_1x3_abl_collapsed.py` - **NEW: LED8 session-type-8 LED-off valid-choice RTD histogram collapsed across `|ILD|`, plotted as a 1x3 ABL grid over `0 <= RTwrtStim <= 1`. Histogram bins explicitly include the configured upper RT bound.**
- `led8_session8_led_off_rtds_by_led_lag.py` - **NEW: LED8 LED-off RTD diagnostic split by lag from the most recent LED-on trial in a 4x3 condition-by-ABL layout. Lag is computed from actual `trial` number differences within `(animal, session)`, not retained-row offsets, so skipped repeat-filtered trials do not misclassify lag.**
- `led8_session8_led_off_rtds_by_led_lag_1x5.py` - **NEW: LED8 LED-off RTD lag overlay collapsed across ABL and plotted over the five `|ILD|` values. Uses trial-number lag and inclusive RT histogram bins.**
- `led8_session8_led_off_rtds_by_led_lag_3x5_overlay.py` - **NEW: LED8 LED-off RTD lag overlay in the full 3x5 `ABL x |ILD|` grid. Uses trial-number lag and inclusive RT histogram bins to test whether recent LED-on trials visibly shift later LED-off RTDs.**
- `led8_session8_led_off_rt_quantiles_by_led_lag_3x5.py` - **NEW: Bootstrap RT quantile confirmation for the LED-on carryover check. Computes q10/median/q90 and 95% bootstrap CIs for `1 lag`, `2 lag`, and `3+ lag` LED-off trials in each `ABL x |ILD|` panel, saving both a figure and CSV summary. Median CIs overlapped for `1 lag` vs `3+ lag` in all 15 conditions, with the largest median spread across lag groups about 21 ms.**
- `led8_session8_led_off_rt_lag_stratified_permutation.py` - **NEW: Stratified permutation test for LED-off RT carryover. Residualizes RT within `(animal, session, ABL, |ILD|)` strata, shuffles lag labels within the same strata, and compares `1 lag`/`2 lag` against `3+ lag`. The observed contrasts were small (`1 lag - 3+ lag` about 0.20 ms, `p = 0.917`; `2 lag - 3+ lag` about 1.26 ms, `p = 0.563`; omnibus `p = 0.836`), supporting no detectable RT carryover.**
- `led8_session8_led_off_accuracy_lag_stratified_permutation.py` - **NEW: Matching stratified permutation test for LED-off accuracy (`success == 1`). Residualizes accuracy within `(animal, session, ABL, |ILD|)` strata and shuffles lag labels within strata. The observed contrasts were small (`1 lag - 3+ lag` about -0.23 percentage points, `p = 0.648`; `2 lag - 3+ lag` about -0.18 percentage points, `p = 0.732`; omnibus `p = 0.873`), supporting no detectable accuracy carryover.**


- `fit_added_noise/psiam_tied_dv_map_utils_with_PDFs.py` - has post LED effect funcs - `stupid_f_integral` and `PA_with_LEDON_2`

## Proactive Process Model Documentation

See [PROACTIVE_MODEL_DOCUMENTATION.md](PROACTIVE_MODEL_DOCUMENTATION.md) for detailed explanation of the proactive process model, theoretical functions, truncation, censoring, and likelihood calculations used for model fitting.
