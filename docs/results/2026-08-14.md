# Results: 2026-08-14

Add result entries below this line.

## LED7 no-truncation lapse: six fits versus aggregate fit

![FCT-style RT-relative-to-LED comparison for six LED7 animals, pooled data with a trial-count-weighted mixture of the six animal-wise models, and the same pooled data with one shared seven-parameter aggregate model. The top row is the regular view (-300 to 400 ms); the bottom row is the zoomed view (-200 to 200 ms). Individual data use 20/10 ms regular/zoom bins, pooled data use 10/5 ms bins, and theory uses 5 ms resolution with 5,000 intact paired t_LED/t_stim draws. All fits use every LED-ON plus LED-OFF row, no 300 ms truncation, and the exponential lapse mixture. Animal columns use 150k audit fits for 92, 99, 100, and 103 and selected 50k fits for 93 and 98. The aggregate SVI completed 150k checks, restored its 101k best checkpoint, and had finite losses and posterior samples.](../assets/results/2026-08-14/led7_no_trunc_exp_lapse_rtwrtled_six_fits_vs_aggregate_2x8.png)

*FCT-style RT-relative-to-LED comparison for six LED7 animals, pooled data with a trial-count-weighted mixture of the six animal-wise models, and the same pooled data with one shared seven-parameter aggregate model. The top row is the regular view (-300 to 400 ms); the bottom row is the zoomed view (-200 to 200 ms). Individual data use 20/10 ms regular/zoom bins, pooled data use 10/5 ms bins, and theory uses 5 ms resolution with 5,000 intact paired t_LED/t_stim draws. All fits use every LED-ON plus LED-OFF row, no 300 ms truncation, and the exponential lapse mixture. Animal columns use 150k audit fits for 92, 99, 100, and 103 and selected 50k fits for 93 and 98. The aggregate SVI completed 150k checks, restored its 101k best checkpoint, and had finite losses and posterior samples.*

Source: `fitting_aborts/plot_numpyro_svi_proactive_led_step_jump_no_trunc_exp_lapse_rtwrtled_2x8.py`
Figure: `docs/assets/results/2026-08-14/led7_no_trunc_exp_lapse_rtwrtled_six_fits_vs_aggregate_2x8.png`
